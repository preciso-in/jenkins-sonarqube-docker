pipeline{
    agent any
    stages{
        stage('checkout'){
            steps{
              git branch: 'main', url: 'https://github.com/nparkhe83/jenkins-sonarqube-docker.git'
            }
        }
    }
}
